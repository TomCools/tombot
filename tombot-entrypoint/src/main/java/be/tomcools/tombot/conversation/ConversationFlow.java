package be.tomcools.tombot.conversation;
/*
public class ConversationFlowDemo {

    public static void test() {
        if(IS_FLOW_ACTIVATION_MESSAGE) {
            //activate flow and ask next details if required
        } else {
            //Save details in context if relevant (like Location)
            if(IS_IN_FLOW) {
                if(MESSAGE_MAKES_SENSE_IN_FLOW) {
                    //complete or ask next details
                } else {
                    //run NLP
                    if(IS_INTENT_FLOW_ACTIVATION) {
                        //Ask confirmation for flow activation. --> results in new Flow Activation Message
                    } else {
                        //fallback --> Euhm, try again, doesn't make sense to me...
                    }
                }
            } else {
                //Determine possible flows (possible using NLP)
                //present flows as Quick Replies
            }
        }
    }
}
*/